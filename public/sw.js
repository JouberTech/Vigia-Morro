/* Estrutura futura de Service Worker, não registrada pelo MVP.
 * Não há interceptação de fetch, cache de dados de risco, push ou sync ativos.
 * Implementar apenas após definir versionamento, validade e consentimento.
 * Não cachear/prefetch tiles públicos do OpenStreetMap para uso offline.
 */
self.addEventListener("install", () => {});
self.addEventListener("activate", () => {});
